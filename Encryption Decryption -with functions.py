#Encryption/Decryption with functions
#Description: Encrypt laintext and decryppt ciphertext using "odd&even" transposition
#Author:AAS

#Date:June 5th 2019

# encrypt(plain)returns cipher
def encrypt(plain) :
    """Encrypts plain message into a cipher and returns it."""
    #code here
    return cipher
# decrypt(cipher) returns plain
    """Decrypts cipher text back into plain text and returns it."""
    #code here
    return plain
#main part of program

#ask the user for a plain text message and assign it to plaintext variable.

#call encrypt(plainText) and assign the return value to the cipherText.

# Print the result.

# Call decrypt(cipherText) and assign return value to plainText

#Print result